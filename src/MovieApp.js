import React from 'react'
import {AppRoutes} from './router/AppRoutes'
/* import { useFetchTrendig } from './hooks/useFetchTrending' */

export const MovieApp = () => {
    /* const{movies, isLoading} = useFetchTrendig ();
    console.log('movies',movies) */
  return (
    <>
       <AppRoutes/>
    </>
   
  )
}
